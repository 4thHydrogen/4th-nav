# OpenSpec Specs

This directory contains the current agreed behavior of the project.

Create capability folders only when behavior is stable enough to document, for example:
- `search/`
- `wallpaper/`
- `icons/`
- `overlay/`
- `folders/`

Small visual tweaks do not need specs here.
