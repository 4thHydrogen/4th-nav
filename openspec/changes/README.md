# OpenSpec Changes

Create one folder per proposed medium or large change.

Recommended structure:

```text
<change-id>/
├── proposal.md
├── tasks.md
└── specs/
    └── <capability>/
        └── spec.md
```

Use `archive/` for completed changes after their requirement deltas have been merged into `openspec/specs/`.
